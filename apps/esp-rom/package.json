{
  "name": "esp-rom",
  "version": "0.1.1",
  "private": true,
  "description": "ESP32S3 固件项目",
  "scripts": {
    "build": "cargo build --release",
    "flash": "cargo run --release",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all -- --check",
    "clippy": "cargo clippy --all-targets --all-features --workspace -- -D warnings",
    "clean": "cargo clean"
  }
}
